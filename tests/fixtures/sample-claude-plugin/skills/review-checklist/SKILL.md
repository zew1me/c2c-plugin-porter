---
name: review-checklist
description: Use when reviewing code changes and preparing actionable findings.
---

# Review Checklist

Use Skill(update-config) if the review reveals a missing configuration default.
AskUserQuestion only when a decision will materially change the rewrite.
